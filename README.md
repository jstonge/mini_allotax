# A minimal example of Allotaxonometer on the web

After having clone the repo and `cd` in it. Open local server, using:

```shell
python3 -m http.server
```

Go to [local server](http://0.0.0.0:8000/).
